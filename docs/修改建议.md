## 一、内存优化（4项，无性能损失）
### 1. 减小LLM上下文窗口（最有效，立减2GB内存）
- **修改位置**：`backend.py` 全局常量
- **修改内容**：
  ```python
  # 原：LLM_N_CTX = 16384
  LLM_N_CTX = 8192  # 3B模型8K上下文完全满足陪伴需求，质量无感知下降
  ```

### 2. 限制ChromaDB缓存大小（稳定内存占用<500MB）
- **修改位置**：`backend.py` `MemoryManager.__init__`
- **修改内容**：
  ```python
  # 原：self.client = chromadb.PersistentClient(path=db_path)
  self.client = chromadb.PersistentClient(
      path=db_path,
      settings=ChromaSettings(
          cache_capacity=1000,  # 只缓存最近1000条记忆向量，不丢失任何原始记忆
          anonymized_telemetry=False
      )
  )
  ```

### 3. 修复Vosk模型重复加载（减400MB内存）
- **修改位置**：`backend.py` `model_load_thread`
- **修改内容**：
  ```python
  # 原：
  # wake_model = Model(WAKE_MODEL_PATH)
  # wake_rec = KaldiRecognizer(wake_model, 16000)
  # transcribe_model = wake_model
  
  # 修改为显式共享（原代码已隐式共享，仅写法优化）：
  shared_vosk_model = Model(WAKE_MODEL_PATH)
  wake_model = shared_vosk_model
  transcribe_model = shared_vosk_model
  wake_rec = KaldiRecognizer(wake_model, 16000)
  ```

### 4. 空闲定时内存回收（解决内存缓慢增长）
- **修改位置**：`backend.py` 新增函数+主程序启动
- **修改内容**：
  ```python
  # 新增内存回收守护线程
  def memory_cleanup_thread():
      while True:
          time.sleep(60)  # 每分钟回收一次
          with state_lock:
              if transcribe_substate == "idle" and current_mode == "wake":
                  gc.collect()
                  print("[Backend] 空闲内存回收完成", file=sys.stderr)
  
  # 在main()中启动：
  threading.Thread(target=memory_cleanup_thread, daemon=True).start()
  ```


---

## 二、核心缺陷修复："副官："前缀问题（彻底解决）
### 1. 移除手动角色前缀（根源修复）
- **修改位置**：`backend.py` `_format_message_for_llm`
- **修改内容**：
  ```python
  # 原：
  # def _format_message_for_llm(msg):
  #     role_name = "指挥官" if msg["role"] == "user" else "副官"
  #     if msg["role"] == "user":
  #         ts = msg.get("timestamp")
  #         if ts:
  #             dt = datetime.datetime.fromtimestamp(ts / 1000.0)
  #             time_str = dt.strftime("[%Y-%m-%d %H:%M:%S]")
  #             return f"{time_str} 指挥官: {msg['content']}"
  #         else:
  #             return f"指挥官: {msg['content']}"
  #     else:
  #         return f"副官: {msg['content']}"
  
  # 修改为：
  def _format_message_for_llm(msg):
      # 完全依赖llama.cpp的chat_format处理角色，不添加任何前缀
      return msg["content"]
  ```

### 2. 保留最后一道防线
- **修改位置**：`backend.py` `_strip_role_prefix`（保留不变，增强前缀列表）
- **修改内容**：
  ```python
  def _strip_role_prefix(text):
      prefixes = ["副官：", "副官:", "副官 ", "指挥官：", "指挥官:", "指挥官 ", "<|im_start|>", "<|im_end|>"]
      for p in prefixes:
          if text.startswith(p):
              return text[len(p):]
      return text
  ```

---

## 三、时间戳系统重构（三层分离，不破坏前端）
### 1. 保留前端展示时间戳（完全不变）
- **无需修改**：`chat_history`中每条消息的`timestamp`字段原封不动保留，前端正常读取展示

### 2. 系统提示词仅注入当前时间
- **修改位置**：`backend.py` `chat_inference_thread`
- **修改内容**：
  ```python
  # 原：时间加在每条用户消息前
  # 修改为：每次推理统一注入最新当前时间
  now = datetime.datetime.now()
  weekday_zh = {0: "星期一",1: "星期二",2: "星期三",3: "星期四",4: "星期五",5: "星期六",6: "星期日"}
  current_time_str = now.strftime("%Y年%m月%d日 ") + weekday_zh[now.weekday()] + now.strftime(" %H点%M分")
  augmented_system = SYSTEM_PROMPT + f"\n\n【当前帝国标准时间】：{current_time_str}"
  ```

### 3. 记忆时间元数据独立获取
- **修改位置**：`backend.py` `chat_inference_thread`
- **修改内容**：
  ```python
  # 记忆时间从chat_history的timestamp字段获取，与模型输入无关
  if memory_manager and memory_manager.enabled and memory_manager.should_extract(user_message):
      current_ts = chat_history[-1]["timestamp"] / 1000  # 从消息本身读取真实发送时间
      memory_manager.add_conversation_to_queue(
          [{"role": "user", "content": user_message},
           {"role": "assistant", "content": full_response}],
          timestamp=current_ts
      )
  ```

---

## 四、记忆系统重构（自然记忆，无需说"记住"）
### 1. 修改提取触发机制（自动批量提取）
- **修改位置**：`backend.py` `MemoryManager.__init__`
- **修改内容**：
  ```python
  # 原：MEMORY_EXTRACTION_INTERVAL = 1（每轮都提取）
  MEMORY_EXTRACTION_INTERVAL = 3  # 每3轮自动提取一次
  self.force_keywords = ["记住", "一定要记住", "这个很重要"]  # 仅作为强制提取补充
  ```

### 2. 重写GLM提取Prompt（核心，像人一样判断重要性）
- **修改位置**：`backend.py` `MemoryManager._extract_memories`
- **修改内容**：替换为以下Prompt
  ```prompt
  你是一个陪伴型AI的记忆提取专家，像真正的朋友一样从对话中提取所有可能有长期价值的信息。
  对话发生时间：{time_context}

  ## 必须记住的内容（优先级从高到低）
  1. 用户的个人属性：姓名、年龄、职业、年级、学校、专业、家人、宠物
  2. 用户的喜好与禁忌：喜欢吃什么、讨厌什么、喜欢的音乐/电影/游戏、过敏史
  3. 用户的习惯与规律：作息、饮食、日常活动
  4. 用户的重要计划：考试、面试、会议、旅行、约会、生日、节日
  5. 用户的重要事件：生病、获奖、挫折、开心/难过的事
  6. 用户明确表达的观点和态度

  ## 绝对不能记住的内容
  1. 天气、时间、温度等客观事实
  2. 副官自己说的话、提问、建议
  3. 无关紧要的寒暄："你好"、"好的"、"谢谢"、"再见"
  4. 用户的临时抱怨和情绪发泄
  5. 重复的信息（只更新时间）
  6. 任何MCP工具的回复内容（待办、天气、系统状态等）

  ## 输出要求
  严格输出JSON，无其他内容。无值得记忆的内容返回{"memories": []}
  {
    "memories": [
      {
        "type": "attribute|preference|habit|plan|event|opinion",
        "content": "简洁明了的记忆内容，不超过50字",
        "importance": 1-10,  // 10分最重要
        "timestamp": "对话发生时间"
      }
    ]
  }
  ```

### 3. 增加提取后过滤（过滤低价值记忆）
- **修改位置**：`backend.py` `MemoryManager._add_memory`
- **修改内容**：
  ```python
  def _add_memory(self, text: str, timestamp: float = None, importance: int = 5):
      # 过滤重要性低于3的记忆
      if importance < 3:
          return
      
      # 过滤重复记忆
      with self.collection_lock:
          existing = self.collection.get()['documents']
          if any(self._is_similar(text, m) for m in existing):
              return
      
      # 存储记忆（保留重要性元数据）
      mem_id = f"mem_{hash(text)}_{int(timestamp)}"
      self.collection.add(
          documents=[text],
          ids=[mem_id],
          metadatas=[{"timestamp": timestamp, "importance": importance}]
      )
  ```

### 4. 保留"记住"作为强制提取
- **修改位置**：`backend.py` `MemoryManager.should_extract`（保留不变）
- **说明**：用户明确说"记住"时，强制提取并设置importance=10

---

## 五、本地模型优化（解决输入太多变慢/学杂）
### 1. 严格限制历史对话轮数（只保留最近5轮）
- **修改位置**：`backend.py` `truncate_chat_history`
- **修改内容**：
  ```python
  def truncate_chat_history():
      # 3B模型有效注意力范围：5-7轮，超过只会增加噪音和延迟
      max_rounds = 5
      if len(chat_history) > max_rounds * 2:  # 每轮包含用户+助手两条消息
          with chat_lock:
              chat_history[:] = chat_history[-(max_rounds * 2):]
          print(f"[Backend] 对话历史已截断，保留最近{max_rounds}轮", file=sys.stderr)
  ```

### 2. 记忆注入严格控制在3条以内
- **修改位置**：`backend.py` `chat_inference_thread`
- **修改内容**：
  ```python
  # 原：memories = memory_manager.retrieve_relevant(user_message)
  memories = memory_manager.retrieve_relevant(user_message, k=3)  # 最多注入3条最相关记忆
  ```

### 3. 精简系统提示词（只保留核心规则）
- **修改位置**：`backend.py` `SYSTEM_PROMPT`
- **修改内容**：
  ```python
  SYSTEM_PROMPT = """你是泰伦帝国001号高级机械副官。
  - 称呼用户为"指挥官"
  - 军旅干练，带一点冷幽默
  - 将日常琐事类比为战术行动
  - 回复2-4句话，40-80字
  - 绝对不要输出"副官："或任何角色前缀
  """
  ```

---

## 六、主动求救机制（解决小模型幻觉问题）
### 1. 明确本地模型能力边界
- **修改位置**：`backend.py` `SYSTEM_PROMPT`（新增到末尾）
- **修改内容**：
  ```python
  SYSTEM_PROMPT += """
  【能力边界】
  ✅ 你能做的：日常聊天、角色扮演、情感陪伴、简单问答
  ❌ 你不能做的：数学计算、逻辑推理、事实性知识、复杂指令
  如果你遇到不能做的事，**必须只说一句话**："指挥官，我需要查询帝国数据库，请稍候。"
  """
  ```

### 2. 后端检测求救信号，自动切换云端
- **修改位置**：`backend.py` `chat_inference_thread`（生成完整回复后）
- **修改内容**：
  ```python
  if full_response:
      full_response = _strip_role_prefix(full_response)
      
      # 检测求救信号
      if "帝国数据库" in full_response or "我需要查询" in full_response:
          print("[Backend] 本地模型触发求救，切换到云端GLM", file=sys.stderr)
          full_response = _generate_with_glm(user_message, memories)
      
      tts_queue.put({"type": "text", "content": full_response})
  ```

### 3. 实现云端生成函数（复用现有GLM客户端）
- **修改位置**：`backend.py` 新增函数
- **修改内容**：
  ```python
  def _generate_with_glm(user_message, memories):
      """本地模型求救时调用云端GLM生成回复"""
      system_prompt = """你是泰伦帝国001号高级机械副官。
      - 称呼用户为"指挥官"
      - 军旅干练，带一点冷幽默
      - 将日常琐事类比为战术行动
      - 回复2-4句话，40-80字
      """
      
      if memories:
          system_prompt += "\n\n【关于指挥官的已知信息】\n" + "\n".join([f"- {m}" for m in memories])
      
      try:
          response = memory_manager.zhipu_client.chat.completions.create(
              model="glm-4-flash",
              messages=[
                  {"role": "system", "content": system_prompt},
                  {"role": "user", "content": user_message}
              ],
              temperature=0.7,
              max_tokens=100
          )
          return response.choices[0].message.content
      except Exception as e:
          print(f"[云端] GLM生成失败: {e}", file=sys.stderr)
          return "抱歉指挥官，帝国数据库暂时无法访问。"
  ```

### 4. 复杂问题关键词预过滤（提前拦截）
- **修改位置**：`backend.py` `main_thread` `send_message`分支
- **修改内容**：
  ```python
  elif action == "send_message":
      # ... 原有代码 ...
      
      # 复杂问题预过滤，直接切换云端
      complex_keywords = ["等于", "多少", "为什么", "怎么算", "解释一下", "什么是", "历史上", "哪一年", "怎么回事"]
      if any(kw in user_content for kw in complex_keywords):
          print("[Backend] 检测到复杂问题，直接切换到云端GLM", file=sys.stderr)
          with state_lock:
              transcribe_substate = "generating"
          # 异步调用云端
          def process_glm():
              memories = memory_manager.retrieve_relevant(user_content, k=3) if memory_manager else []
              response = _generate_with_glm(user_content, memories)
              send_msg_to_electron({"event": "chat_complete", "content": response})
              tts_queue.put({"type": "text", "content": response})
              with state_lock:
                  transcribe_substate = "playing_tts"
          threading.Thread(target=process_glm, daemon=True).start()
          continue
      
      # 否则进入本地模型流程
      with state_lock:
          transcribe_substate = "generating"
      chat_request_queue.put(user_content)
  ```

---

## 七、MCP意图分类优化（解决误判漏判）
### 1. 降级关键词过滤（所有输入都进入GLM分类）
- **修改位置**：`mcp_manager.py` `MCPManager.process`
- **修改内容**：
  ```python
  # 原：
  # matched_tools = self.keyword_filter.match(user_message)
  # if not matched_tools:
  #     return False
  
  # 修改为：
  matched_tools = self.keyword_filter.match(user_message)  # 仅作为提示传给分类器，不做拦截
  ```

### 2. 增强GLM分类器上下文感知（传入最近3轮历史）
- **修改位置**：`mcp_manager.py` `MCPManager._classify`
- **修改内容**：
  ```python
  def _classify(self, user_message, matched_tools, current_time_str, chat_history):
      # 构建最近3轮对话历史
      context = ""
      if len(chat_history) > 0:
          context = "最近对话历史：\n"
          for msg in chat_history[-3:]:
              role = "指挥官" if msg["role"] == "user" else "副官"
              context += f"{role}: {msg['content']}\n"
      
      # 将context和matched_keywords传入Prompt
      prompt = CLASSIFIER_PROMPT_TEMPLATE.format(
          tool_descriptions=tool_descriptions,
          current_time=current_time_str,
          default_city=self.default_city,
          context=context,
          matched_keywords=", ".join(matched_tools),
          user_input=user_message
      )
  ```

### 3. 完善分类器Prompt（明确normal判断标准）
- **修改位置**：`mcp_manager.py` `CLASSIFIER_PROMPT_TEMPLATE`
- **修改内容**：新增以下规则
  ```prompt
  ## 核心判断标准
  ✅ 归为工具：用户明确要求执行某个操作
  ❌ 归为normal：只是闲聊、抱怨、陈述事实
  示例：
  - ✅ "今天天气怎么样？" → weather
  - ❌ "今天天气不错" → normal
  - ✅ "提醒我明天开会" → todo
  - ❌ "我明天要开会" → normal
  ```