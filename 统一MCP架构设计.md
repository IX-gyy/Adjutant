\# 统一MCP工具链架构设计（整合TODO+4大新功能）

完全基于你现有TODO系统的"\*\*关键词预过滤+GLM全接管+本地模型零感知+记忆完全隔离\*\*"核心范式，设计一套可扩展的统一MCP工具链。所有功能共享同一套分类、路由、状态管理和通信机制，原TODO系统直接迁移至此架构，无需重构核心逻辑。



\## 一、核心架构总览

\### 1. 统一处理流程（完全复用现有generating状态）

```

用户输入 → 【MCP总开关检查】→ 【本地关键词预过滤】→ 【GLM意图分类器】

&#x20;          ↓（命中MCP）                          ↓（未命中）

&#x20;   【MCP任务分发器】→ 对应工具处理函数        进入本地LLM正常对话流程

&#x20;          ↓

&#x20;   【GLM结果生成器】→ 固定格式回复

&#x20;          ↓

&#x20;   【前端推送+TTS播报】→ 切换回idle状态

```



\### 2. 核心设计原则（严格遵守）

\- \*\*本地零智能\*\*：所有意图判断、参数解析、结果生成100%由GLM-4-Flash完成，后端仅执行"原子操作"

\- \*\*记忆完全隔离\*\*：所有MCP相关对话\*\*不写入短期记忆、不送入长期记忆提取队列、本地模型完全不可见\*\*

\- \*\*状态机复用\*\*：所有MCP任务都在`transcribe\_substate = "generating"`状态下执行，复用现有锁、取消事件和队列机制

\- \*\*统一错误处理\*\*：所有工具的错误都返回标准化格式，由GLM统一生成用户友好的错误提示

\- \*\*可插拔扩展\*\*：新增工具只需添加关键词规则、处理函数和GLM Prompt，无需修改核心架构



\## 二、统一MCP分类器机制（核心改进）

采用"\*\*本地快速预过滤+GLM精准分类\*\*"两级架构，兼顾响应速度和准确性，同时最小化GLM调用次数。



\### 1. 第一级：本地关键词预过滤（毫秒级）

在Python后端维护一个静态关键词字典，用户输入先经过此过滤，只有命中关键词的才会进入GLM分类阶段。

```python

\# 本地关键词预过滤字典（可配置）

MCP\_KEYWORDS = {

&#x20;   "todo": \["提醒", "待办", "任务", "安排", "别忘了", "要做的事"],

&#x20;   "weather": \["天气", "气温", "下雨", "下雪", "晴天", "阴天", "温度", "穿衣建议"],

&#x20;   "system\_status": \["电脑", "系统", "CPU", "内存", "磁盘", "电量", "网络", "卡", "慢"],

&#x20;   "time\_tool": \["时间", "几点", "日期", "几号", "星期", "倒计时", "计时", "还有多少天"],

&#x20;   "web\_search": \["查一下", "搜索", "找资料", "最新消息", "新闻", "百科", "怎么回事"]

}

```

\- 匹配规则：用户输入包含任意一个关键词即命中对应类别

\- 冲突处理：同时命中多个关键词时，标记为"多意图"，进入GLM分类阶段

\- 性能优化：使用正则表达式预编译所有关键词，单次匹配耗时<1ms



\### 2. 第二级：GLM意图分类器（精准判断）

对于命中关键词或多意图的输入，调用GLM-4-Flash进行最终意图分类和参数提取。\*\*这是整个MCP系统唯一需要编写的GLM Prompt\*\*，所有工具共享。



```prompt

\# 泰伦帝国副官MCP意图分类器 v1.0

你是一个严格的意图分类器，只输出JSON格式，不添加任何其他内容。



\## 任务

分析用户输入，判断其属于以下哪个工具类别，并提取必要的参数。



\## 工具类别及参数说明

1\. todo: 待办事项管理

&#x20;  - 子操作: add(添加), list(查询), complete(完成), delete(删除)

&#x20;  - 参数: content(待办内容), due\_date(截止时间, ISO格式), todo\_id(待办ID)

2\. weather: 天气查询

&#x20;  - 参数: location(地点, 为空表示默认城市), time\_range(时间范围: today/tomorrow/week)

3\. system\_status: 系统状态查询

&#x20;  - 子操作: cpu, memory, disk, battery, network, all(全部)

4\. time\_tool: 时间工具

&#x20;  - 子操作: current\_time(当前时间), date\_calc(日期计算), countdown(倒计时), stopwatch(秒表)

&#x20;  - 参数: duration(倒计时时长, 分钟), target\_date(目标日期)

5\. web\_search: 网页搜索

&#x20;  - 参数: query(搜索关键词)

6\. normal: 不属于任何MCP工具，进入正常对话流程



\## 规则

1\. 严格按照上述类别分类，不得新增类别

2\. 若参数不明确，在params中注明"missing": \["参数名"]

3\. 若用户同时请求多个操作，返回第一个最明确的操作

4\. 绝对禁止输出任何JSON以外的内容



用户输入: {user\_input}

```



\### 3. 分类结果处理

\- 若GLM返回`"tool": "normal"`：将用户输入转发给本地LLM，进入正常对话流程

\- 若GLM返回其他工具类别：将分类结果和参数传递给MCP任务分发器，进入对应工具处理流程

\- 若GLM返回参数缺失：直接由GLM生成提示用户补充信息的回复，无需调用工具



\## 三、各功能详细机制设计

\### 1. 改进版TODO系统（原系统迁移）

\#### 核心变化

\- 原有的关键词拦截逻辑迁移至统一MCP分类器

\- 原有的GLM处理逻辑拆分为"参数提取"和"结果生成"两部分，参数提取由分类器完成

\- 保留原有的"本地模型不感知、长期记忆不存储"原则

\- 保留原有的"完成/删除仅支持前端手动操作"限制



\#### 处理流程

```

用户输入 → 分类器返回{"tool":"todo", "sub\_op":"add", "params":{"content":"...", "due\_date":"..."}}

&#x20;          ↓

MCP分发器 → 调用todo\_handler.add\_todo(content, due\_date)

&#x20;          ↓

后端执行SQLite插入操作 → 返回todo对象

&#x20;          ↓

GLM结果生成器 → 生成固定格式回复："已为您添加待办事项：【XX】，截止时间：XX"

&#x20;          ↓

推送前端+TTS播报

```



\#### 新增：待办到期提醒机制（你最需要的功能）

这是对现有TODO系统的核心增强，完全在后端实现，不依赖本地模型。

\- \*\*应用启动检查\*\*：后端启动时（`full\_ready`事件后），立即查询SQLite中所有`status="pending"`且`due\_date <= 当前时间`的待办事项

\- \*\*后台定时检查\*\*：启动一个独立的`todo\_reminder\_thread`，每分钟检查一次到期待办

\- \*\*提醒方式\*\*：

&#x20; 1. 推送`todo\_reminder`事件给前端，前端弹出系统通知（Electron Notification API）

&#x20; 2. 自动播放TTS提醒："指挥官，您有一条待办事项已到期：【XX】"

&#x20; 3. 在聊天窗口自动插入一条待办提醒消息

\- \*\*提醒去重\*\*：每个待办事项最多提醒3次，每次间隔10分钟，避免骚扰

\- \*\*状态更新\*\*：用户点击通知或聊天窗口中的"完成"按钮，自动更新待办状态为`completed`



\### 2. 天气查询功能

\#### 触发机制

\- 关键词：天气、气温、下雨、下雪、晴天、阴天、温度、穿衣建议

\- 示例："今天北京天气怎么样"、"明天会下雨吗"、"这周的天气预报"



\#### 处理流程

```

分类器返回{"tool":"weather", "params":{"location":"北京", "time\_range":"today"}}

&#x20;          ↓

后端获取用户当前位置（可选）→ 若location为空，使用默认位置

&#x20;          ↓

调用和风天气API → 获取结构化天气数据

&#x20;          ↓

GLM结果生成器 → 按固定格式生成回复：

&#x20; "【XX市今日天气】

&#x20;  天气：晴转多云

&#x20;  气温：18-28℃

&#x20;  风向：东北风3级

&#x20;  建议：天气晴朗，适合外出，建议穿薄外套。"

&#x20;          ↓

推送前端+TTS播报

```



\#### 关键细节

\- \*\*位置获取\*\*：前端通过浏览器IP定位API获取用户当前城市，应用启动时发送给后端保存为默认位置

\- \*\*API缓存\*\*：对同一地点同一时间范围的查询结果缓存1小时，减少API调用

\- \*\*错误处理\*\*：API调用失败或位置无效时，返回标准化错误提示

\- \*\*记忆隔离\*\*：天气查询结果不写入任何记忆，仅在前端展示



\### 3. 系统状态查询功能

\#### 触发机制

\- 关键词：电脑、系统、CPU、内存、磁盘、电量、网络、卡、慢

\- 示例："我的电脑卡不卡"、"还有多少电量"、"查看系统状态"



\#### 处理流程

```

分类器返回{"tool":"system\_status", "sub\_op":"all"}

&#x20;          ↓

后端调用psutil库获取系统状态数据：

&#x20; {

&#x20;   "cpu": {"usage": 35, "cores": 8},

&#x20;   "memory": {"used": "12GB", "total": "32GB", "usage": 37},

&#x20;   "disk": {"used": "256GB", "total": "512GB", "usage": 50},

&#x20;   "battery": {"percent": 75, "time\_left": "3小时20分钟"},

&#x20;   "network": {"status": "connected", "ip": "192.168.1.100"}

&#x20; }

&#x20;          ↓

GLM结果生成器 → 生成副官风格的系统状态报告

&#x20;          ↓

推送前端+TTS播报

```



\#### 安全边界

\- 100%只读操作，不修改任何系统设置

\- 不返回敏感信息（如完整进程列表、网络密码）

\- 进程查询仅返回占用资源最高的前5个进程名和CPU/内存占用

\- 禁止任何形式的系统操作（如结束进程、清理磁盘）



\### 4. 时间管理工具

\#### 触发机制

\- 关键词：时间、几点、日期、几号、星期、倒计时、计时、还有多少天

\- 示例："现在几点了"、"距离6月18日还有多少天"、"设置25分钟倒计时"



\#### 子功能详细机制

1\. \*\*当前时间/日期查询\*\*

&#x20;  - 后端直接获取系统时间，GLM格式化为自然语言："当前时间是2026年5月16日星期六下午3点45分"

&#x20;  - 这是所有功能中最快的，无需调用GLM分类器，本地直接处理



2\. \*\*日期计算\*\*

&#x20;  - GLM解析目标日期，后端计算差值，GLM生成结果："距离2026年6月18日还有33天"



3\. \*\*倒计时\*\*

&#x20;  - 后端启动独立倒计时线程，结束时推送`countdown\_complete`事件

&#x20;  - 前端弹出系统通知并播放TTS提醒："指挥官，您设置的25分钟倒计时已结束"

&#x20;  - 倒计时仅在当前会话有效，关闭窗口自动取消



4\. \*\*秒表\*\*

&#x20;  - 支持开始、暂停、继续、重置操作

&#x20;  - 后端维护秒表状态，前端实时更新显示



\### 5. 网页查询功能

\#### 触发机制

\- \*\*显式触发\*\*：用户输入包含"查一下"、"搜索"、"找资料"、"最新消息"等关键词

\- \*\*隐式触发（可选）\*\*：GLM判断用户问题需要最新信息或外部知识时，自动触发搜索（默认关闭，可配置）

\- \*\*禁止自动触发\*\*：默认情况下，只有用户明确要求搜索时才会调用网页查询功能，避免干扰日常对话



\#### 处理流程

```

分类器返回{"tool":"web\_search", "params":{"query":"2026年星际争霸2比赛结果"}}

&#x20;          ↓

后端调用网页搜索API → 获取前5条搜索结果（标题+摘要+链接）

&#x20;          ↓

GLM结果生成器 → 清洗内容、总结核心信息，按固定格式生成回复：

&#x20; "根据最新搜索结果：

&#x20;  1. 2026年WCS全球总决赛于5月10日结束，韩国选手Serral获得冠军

&#x20;  2. 中国选手TIME获得亚军，创造了中国选手在WCS历史上的最好成绩

&#x20;  3. 下一届WCS比赛将于2026年11月在上海举行

&#x20;  

&#x20;  信息来源：星际争霸2官方网站、网易游戏频道"

&#x20;          ↓

推送前端+TTS播报

```



\#### 关键细节

\- \*\*API选型\*\*：推荐使用百度搜索开放API或360搜索API，均有免费额度

\- \*\*内容清洗\*\*：GLM负责过滤广告、低质内容和重复信息

\- \*\*来源标注\*\*：所有搜索结果必须标注来源，增加可信度

\- \*\*结果长度控制\*\*：回复长度控制在300字以内，避免信息过载

\- \*\*记忆隔离\*\*：搜索结果不写入任何记忆，仅在前端展示



\## 四、架构扩展与状态机适配

\### 1. 后端代码结构扩展

在现有backend目录下新增`mcp/`子目录，统一管理所有MCP相关代码：

```

backend/

├── mcp/

│   ├── \_\_init\_\_.py

│   ├── mcp\_manager.py      # MCP核心管理器（分类、分发）

│   ├── keyword\_filter.py   # 本地关键词预过滤器

│   ├── tools/              # 各工具实现

│   │   ├── todo\_tool.py    # 改进版TODO工具

│   │   ├── weather\_tool.py # 天气查询工具

│   │   ├── system\_tool.py  # 系统状态工具

│   │   ├── time\_tool.py    # 时间管理工具

│   │   └── web\_tool.py     # 网页查询工具

│   └── prompts/            # GLM Prompt模板

│       ├── classifier\_prompt.txt

│       └── result\_prompt.txt

└── ...

```



\### 2. 状态机适配

所有MCP任务都在`transcribe\_substate = "generating"`状态下执行，与原TODO系统完全一致：

\- 进入generating状态时，锁定交互，仅允许取消操作

\- 任务执行过程中，若用户点击"取消"按钮，触发`cancel\_generation\_event`

\- 任务完成后，根据是否有TTS播报，切换至`playing\_tts`或`idle`状态

\- 所有工具处理函数都必须支持取消操作，及时释放资源



\### 3. 前后端通信协议扩展

\#### 后端→前端新增事件

| 事件                | 参数                                  | 说明                     |

|---------------------|---------------------------------------|--------------------------|

| todo\_reminder       | todo: {id, content, due\_date}         | 待办到期提醒             |

| countdown\_complete  | duration: int                         | 倒计时结束提醒           |

| weather\_result      | data: {location, forecast, tips}      | 天气查询结果             |

| system\_status\_result| data: {cpu, memory, disk, battery, network} | 系统状态结果       |

| web\_search\_result   | data: {query, results: list}          | 网页搜索结果             |



\## 五、落地实施步骤

建议按以下顺序逐步实施，每一步都可以独立运行和测试：



\### 第一阶段（1天）：搭建统一MCP架构

1\. 实现本地关键词预过滤器

2\. 实现GLM意图分类器

3\. 实现MCP任务分发器

4\. 将原TODO系统迁移至新架构

5\. 测试现有TODO功能是否正常工作



\### 第二阶段（1天）：实现基础工具

1\. 实现系统状态查询工具

2\. 实现时间管理工具（当前时间、日期计算）

3\. 测试这两个工具的分类、处理和回复流程



\### 第三阶段（1-2天）：实现天气查询和待办提醒

1\. 集成和风天气API，实现天气查询工具

2\. 实现待办到期提醒机制（应用启动检查+后台定时检查）

3\. 测试提醒功能的准确性和稳定性



\### 第四阶段（2天）：实现网页查询功能

1\. 集成网页搜索API

2\. 实现网页查询工具和GLM结果总结

3\. 测试搜索结果的准确性和相关性



\## 六、安全与性能优化

1\. \*\*GLM调用限流\*\*：对MCP分类器和结果生成器的调用进行限流，避免超出API额度

2\. \*\*超时处理\*\*：所有外部API调用设置5秒超时，避免阻塞状态机

3\. \*\*错误重试\*\*：对临时网络错误进行最多2次重试，提高成功率

4\. \*\*日志记录\*\*：记录所有MCP工具的调用日志（时间、工具、参数、结果），便于问题排查

5\. \*\*功能开关\*\*：为每个MCP工具添加独立的开关，用户可以自主开启或关闭

