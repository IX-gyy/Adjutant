我正在做一个ai助手项目，TECH_DESIGN.md是我的项目文档，backend.py是我的后端文件，
我的后端原结构如下，使用pyinstaller打包后变成backend文件夹，整体放到frontend中
backend/
|_models/ 存放模型文件
|_backend.py
|_backend.spec pyinstaller打包文件

而现在，我想要添加一个彩蛋系统，其具体设计如下，请你帮忙看看是否合理：
前置基础约定
全局彩蛋开关：只有开关处于「开启」状态时，才会进入彩蛋匹配流程；开关关闭时，所有输入完全走原有正常对话流程，彩蛋系统完全不生效
强制前缀校验：所有彩蛋触发，必须满足用户输入以「副官」/「副官，」开头，否则直接跳过彩蛋匹配
完整触发全流程（6 个核心步骤）
步骤 1：用户输入发起（前端环节）
用户在输入框输入文本（或语音转写后的文本），点击发送按钮
前端完全按照原有逻辑，向后端发送send_message指令，携带用户输入的文本内容，不做任何额外拦截
核心逻辑：前端只负责发送指令和接收事件渲染，所有校验、匹配逻辑都集中在后端，避免前后端逻辑分散
步骤 2：后端前置双重校验（后端环节，彩蛋系统的第一道闸门）
后端接收到send_message指令后，先不进入原有 LLM 生成流程，先进入彩蛋系统的前置校验环节
第一重校验：检查全局彩蛋开关状态 → 开关关闭，直接跳出彩蛋流程，进入原有正常 LLM 对话流程
第二重校验：检查用户输入是否以“副官”/“副官，”开头 → 不满足，直接跳出彩蛋流程，进入原有正常 LLM 对话流程
双重校验都通过，才会进入下一步彩蛋规则匹配环节
步骤 3：本地 JSON 规则匹配（后端核心环节，完全不碰模型）
后端读取本地的彩蛋规则配置 JSON 文件，按预设的优先级，对用户输入的文本进行匹配
匹配优先级（从高到低，匹配到高优先级规则后，不再匹配低优先级）：
精准指令匹配：100% 完全匹配用户输入的整句话，无模糊匹配，用于隐藏高稀有度彩蛋
场景关键词匹配：必须同时命中 2 组及以上关键词，避免单字误触，用于日常自然触发
简化关键词匹配：必须命中至少 1 个≥2 字的核心关键词，低门槛易探索
匹配结果分支：
匹配成功：锁定对应彩蛋的音频文件、副官过渡语文本、聊天展示内容，进入下一步
匹配失败：直接跳出彩蛋流程，进入原有正常 LLM 对话流程
步骤 4：后端推送事件，前端渲染展示（前后端联动环节）
匹配成功后，后端完全不进入 LLM 生成流程，直接向前端推送专属的彩蛋触发事件（egg_triggered，完全兼容你现有的事件体系）
事件携带参数：副官过渡语文本、蒙斯克语音展示文本、蒙斯克音频文件标识
前端接收到事件后，按照原有聊天消息的渲染逻辑，在聊天窗口依次展示两条消息：
副官头像 + 普通消息样式：「收到元首加密通讯，请聆听最高指示」（你定的过渡语）
蒙斯克专属头像 + 帝国红金样式 +【元首通讯】标识：对应彩蛋的语音文本内容
前端渲染完成后，进入音频播报环节
步骤 5：音频播报全流程（完全复用现有 TTS 能力，无额外开发）
播报顺序严格遵循：先播放副官过渡语的 TTS，再播放蒙斯克的预录语音
具体执行逻辑：
后端先把副官过渡语送入你现有的 TTS 播报队列，复用现有 TTS 合成 / 播放逻辑，用副官默认音色播报
副官过渡语播报完成后，后端直接读取本地预录的蒙斯克音频文件，送入同一个 TTS 播报队列，复用现有的播放、暂停、停止逻辑
整个播报过程，完全复用你现有的playing_tts状态、锁机制、取消事件，用户点击「停止播报」按钮，可直接中断整个播报流程
核心优势：完全不用新增音频播放逻辑，所有能力都是你现有已经实现的，只需要把预录音频当成已合成好的 TTS 音频送入队列即可
步骤 6：流程结束与状态复位
蒙斯克语音播报完成后，后端向前端推送tts_complete事件（复用现有事件）
后端自动复位状态，切回transcribe_substate = idle空闲状态，解锁所有交互
整个彩蛋流程结束，完全不影响用户后续的正常对话
兜底跳转流程
只要以下任意一个条件不满足，就会完全跳出彩蛋流程，100% 走你现有的正常 LLM 对话流程，对原有功能没有任何影响：
彩蛋开关处于关闭状态
用户输入没有“副官”前缀
彩蛋规则匹配失败

后端文件夹原始结构变为：
backend/
|_config/ 存放彩蛋匹配json文件
|_asserts/audio/ 存放彩蛋音频文件的文件夹
其余部分不变

彩蛋规则我在下面给出，每一条规则对应一个音频文件，开头的词语即音频文件名（省略.mp3后缀），括号内是音频的内容，后续是建议匹配场景：
所有匹配规则均强制要求用户输入以「副官」或「副官，」为前缀，仅在彩蛋总开关开启时生效，匹配优先级为精准指令匹配＞场景关键词匹配＞简化关键词匹配，匹配成功后完全不走LLM模型，直接触发对应语音播报。

cherish（任何值得拥有的东西，都来之不易）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「放弃、坚持不下去、太难了、好累」这类负面情绪核心词，以及「想、值得吗、撑不住」这类辅助词即可触发，适配用户吐槽任务难度大、工作学习受挫、想要放弃、抱怨付出没有回报的场景，比如用户输入「副官，我坚持不下去了」「副官，做这个项目太难了」都可触发。

confident（我至始至终，都信心十足）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「没信心、没把握、能行吗、怕搞砸、不确定」这类自我怀疑核心词，以及「我、会不会、能不能」这类辅助词即可触发，适配用户对即将开展的任务缺乏底气、自我怀疑、询问事情可行性的场景，比如用户输入「副官，我怕搞砸这个项目」「副官，我没信心做好这件事」都可触发。

danger（豺狼，都跑到我们家门口了！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「CPU占用高、内存满了、磁盘满了、电脑卡了、死机了」这类系统状态核心词，适配用户查询电脑系统状态、吐槽设备卡顿、资源占用过高、遇到系统故障的场景，比如用户输入「副官，看看我CPU占用多少」「副官，电脑卡死了！」都可触发。

determination（我们的伟大，在于我们的决心！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「开工了、冲了、加油、定个目标、开始做」这类启动动作核心词，以及「写代码、做项目、学习、工作、完成任务」这类目标词即可触发，适配用户宣布开工、立下目标、给自己打气、开启专注模式的场景，比如用户输入「副官，我要开工写代码了」「副官，今天一定要完成这个目标」都可触发。

employ（[广播声]帝国武装力量，无数的优秀儿女时刻守卫着我们的家园大门，但是他们孤木难支，凡是今天应征入伍者，所获的刑法罪责，减半！）采用精准指令匹配，仅当用户输入完全等于「副官，帝国征兵令」「副官，播放征兵广播」时触发，无模糊匹配，作为高稀有度隐藏彩蛋，适配用户主动探索彩蛋、输入专属指令的场景。

encourage（帝国优秀的儿女们，我将引领你们永远前进！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「定时提醒、30分钟后提醒我、到点叫我、专注模式」这类功能触发核心词，以及「写代码、工作、学习、开会」这类目标词即可触发，也支持用户主动输入「副官，给我点鼓励」直接触发，适配用户设置定时提醒、开启专注模式、开工前寻求正向激励的场景，还可联动定时提醒功能，在用户设置的提醒时间到点时自动触发。

focus（认真执行命令，早点收兵）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「摸鱼了、划水了、分心了、不想干了、拖延症」这类状态核心词，以及「还有任务、没写完、没做完、要专注」这类辅助词即可触发，适配用户主动承认自己摸鱼分心、有拖延行为、需要督促专注的场景，比如用户输入「副官，我又摸鱼了，督促我一下」「副官，我拖延症犯了」都可触发。

home（[广播声]凯莫瑞安联合体的经济崩溃迫在眉睫，对于所有想要离开那片废土、去寻找更美好生活的人来说，克哈，是你们所有人安全的港湾！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「好累、想休息、下班了、回家了」这类状态核心词，以及「工作、项目、代码、忙完了」这类辅助词即可触发，适配用户结束一天工作、下班、吐槽身心俱疲想要休息、摸鱼放松的场景，比如用户输入「副官，终于下班了」「副官，今天忙完太累了」都可触发。

invaluable_time（我们每浪费一刻钟，失败的概率就越大！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「赶deadline、快到点了、来不及了、要超时了、倒计时」这类时间紧迫核心词，以及「任务、项目、提交、交作业、要截止了」这类目标词即可触发，适配用户赶项目截止日期、倒计时临近、吐槽时间不够、即将超时提交任务的场景，比如用户输入「副官，项目要截止了来不及了」「副官，还有半小时就要交了」都可触发。

launch（发射导弹，继续发！全射出去！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「提交代码、发版、上线、部署、发送、提交」这类动作核心词，以及「项目、代码、文档、邮件、版本」这类对象词即可触发，也支持用户主动输入「副官，发射导弹」直接触发，适配用户提交代码、项目上线发版、部署程序、发送重要邮件或文档的场景，比如用户输入「副官，我要提交代码上线了」「副官，把这个邮件发出去」都可触发。

noisy（别让他们妨碍我们！拍死周围这些嗡嗡叫的“苍蝇”！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「好吵、太多干扰、广告弹窗、消息太多、烦死了」这类干扰源核心词，以及「写代码、工作、学习、专注、静不下心」这类需求词即可触发，适配用户吐槽环境嘈杂、弹窗广告多、消息干扰频繁、无法静下心专注做事的场景，比如用户输入「副官，消息太多太吵了静不下心」「副官，好多弹窗好烦」都可触发。

people（这场战争已经没有平民可言！立即增援前线！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「顶不住了、扛不住了、忙不过来了、需求太多了、改不完了」这类压力过载核心词，以及「项目、工作、代码、需求、忙死了」这类对象词即可触发，适配用户吐槽工作项目需求过多、忙不过来、压力过大顶不住的场景，比如用户输入「副官，需求太多我顶不住了」「副官，忙不过来了怎么办」都可触发。

people2（帝国的子民们，拿起武器，保卫你们的家园！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「遇到bug了、系统崩了、代码报错了、搞不定了」这类故障核心词，以及「求助、怎么办、怎么解决、修不好」这类需求词即可触发，适配用户遇到代码bug、程序报错、系统故障，寻求解决办法的场景，比如用户输入「副官，代码出bug了搞不定」「副官，系统崩了怎么办」都可触发。

protect（[广播声]我已经驯服了异虫，荡平了星灵，如今，他们的创造者想要夺走我们拥有的一切！永远记住，谁才是最能保护你们的人！）采用精准指令匹配，仅当用户输入完全等于「副官，元首保护宣言」「副官，谁能保护我们」时触发，无模糊匹配，作为高稀有度隐藏彩蛋，也可作为系统遇到重大故障、程序崩溃重启时的兜底触发内容。

radiation（把他们变成哥斯拉！辐射区内的敌人都得死！）采用场景关键词匹配，用户输入带「副官」前缀，同时命中「清空回收站、格式化、删除文件、清理垃圾、卸载软件、杀掉进程」这类清理删除动作核心词，以及「彻底、全部、全删了、清空」这类范围词即可触发，也支持用户主动输入「副官，启动辐射净化」直接触发，适配用户执行清空回收站、格式化磁盘、批量删除文件垃圾、杀掉多余系统进程的场景，比如用户输入「副官，把这些垃圾文件全删了」「副官，清空回收站」都可触发。

urge（你在磨蹭什么？！）采用精准指令匹配，仅当用户输入完全等于「副官，我又磨蹭了」「副官，你在磨蹭什么」时触发，无模糊匹配，作为高门槛的严厉督促类彩蛋，适配用户主动承认自己拖延磨洋工、寻求高强度督促的场景，从根源避免误触。

victory（胜利，没有捷径。享受这种感觉吧，无论它变得多么熟悉，哈哈哈哈……）采用简化关键词匹配，用户输入带「副官」前缀，命中「胜利、搞定了、完成了、成功了、上线了、bug修好了」这类核心词即可触发，适配用户宣布完成任务、修好bug、项目上线成功、达成既定目标的场景，比如用户输入「副官，我搞定了」「副官，项目上线成功了」都可触发。

总有刁民想害朕（为什么总有刁民想害朕？）采用终极精准指令匹配，仅当用户输入完全等于「副官，总有刁民想害朕」「副官，为什么总有刁民想害朕」时触发，无任何模糊匹配，零误触，作为终极隐藏彩蛋，适配用户吐槽被坑、被甩锅、程序出现玄学bug、被人针对的场景。